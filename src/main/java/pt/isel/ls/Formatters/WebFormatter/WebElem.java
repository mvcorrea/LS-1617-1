package pt.isel.ls.Formatters.WebFormatter;

public class WebElem {
    WebInterface x;
    public WebElem(WebInterface x){ this.x = x; }
    public WebInterface getContent(){ return x; }
    //public void addContent(WebInterface y){ }
}
